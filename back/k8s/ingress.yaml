apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: polizai-back
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: api.poliz.ai
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: polizai-back
              port:
                number: 80
