apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: polizai-front
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: poliz.ai
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: polizai-front
              port:
                number: 80
